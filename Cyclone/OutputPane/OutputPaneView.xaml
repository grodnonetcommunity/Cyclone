<UserControl x:Class="AV.Cyclone.OutputPane.OutputPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer x:Name="OutputPaneScrollViewer" Grid.IsSharedSizeScope="True">
        <ScrollViewer.LayoutTransform>
            <ScaleTransform ScaleX="{Binding Path=Model.ZoomLevel}" ScaleY="{Binding Path=Model.ZoomLevel}" />
        </ScrollViewer.LayoutTransform>
        <ItemsControl x:Name="ItemsControl"
                      HorizontalContentAlignment="Stretch" Grid.IsSharedSizeScope="True" />
    </ScrollViewer>
</UserControl>
